<%# encoding: utf-8 %>
<%
  page_title    = "#{category} — #{SITE_TITLE}"
  description   = "Posts in #{category}."
  canonical_url = "#{SITE_URL}/categories/#{category}.html"
  og_type       = 'website'
  og_title      = nil; og_image = nil; date_iso = nil
-%>
<!DOCTYPE html>
<html lang="en">
<head>
<%= renderer.partial('head',
      page_title: page_title, description: description,
      canonical_url: canonical_url, og_type: og_type, og_title: og_title,
      og_image: og_image, date_iso: date_iso, root: root) %>
</head>
<body>
<%= renderer.partial('header', root: root) %>
  <div class="page-body">
    <main id="main-content">
      <header class="page-hero container">
        <nav class="breadcrumb" aria-label="Breadcrumb">
          <a href="<%= root %>blog.html">Writing</a>
          <span class="breadcrumb__sep" aria-hidden="true">&#x203A;</span>
          <span class="breadcrumb__current"><%= h(category) %></span>
        </nav>
        <h1 class="page-hero__title"><%= h(category) %></h1>
        <p class="page-hero__meta"><%= posts.length %> posts</p>
      </header>

      <ol class="blog-list" reversed aria-label="Posts in <%= h(category) %>">
<%- posts.each do |post| -%>
        <%= renderer.partial('post_card', post: post, root: root) %>
<%- end -%>
      </ol>
    </main>
<%= renderer.partial('footer', root: root) %>
  </div>
  <script defer src="<%= root %>javascript/main.js"></script>
</body>
</html>
